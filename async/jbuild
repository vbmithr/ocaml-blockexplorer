(library
 ((name        blockexplorer_async)
  (public_name blockexplorer.async)
  (modules (blockexplorer_async))
  (optional)
  (libraries (hex ocplib-json-typed ezjsonm blockexplorer cohttp-async))))

(executable
 ((name bx)
  ;; (public_name bx_async)
  (modules (bx))
  (libraries (blockexplorer.async))))
